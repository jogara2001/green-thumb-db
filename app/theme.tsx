import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#3A5935'
    },
    secondary: {
      main: '#FFFFFF'
    }
  }
})

export default theme
